# Install required packages for social authentication

```bash
npm install @google-cloud/oauth2 react-google-login react-facebook-login-component
# or
npm install next-auth
```

For Google: Get client ID from Google Cloud Console
For Facebook: Get app ID from Facebook Developers